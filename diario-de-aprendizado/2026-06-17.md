# 📅 Registro de Estudos - 17/06/2026

## 📚 Conteúdo estudado

Hoje dei continuidade aos estudos de SQL Server com foco em relacionamentos entre tabelas e consultas utilizando `INNER JOIN`.

### Assuntos praticados

- Criação da tabela `Livros`
- Inserção de registros (`INSERT INTO`)
- Alteração de tabelas (`ALTER TABLE`)
- Criação de chave estrangeira (`FOREIGN KEY`)
- Atualização de registros (`UPDATE`)
- Consultas com `SELECT`
- Uso do operador `LIKE`
- Funções de agregação (`COUNT`, `MAX`, `AVG`)
- Primeiros exercícios envolvendo `INNER JOIN`

---

## ✅ Aprendizados

- Entendi melhor que a chave estrangeira deve ser criada na tabela que referencia outra tabela.
- Aprendi que o `REFERENCES` precisa informar a tabela e a chave primária correspondente.
- Reforcei o conceito de relacionamento entre tabelas através das chaves primária e estrangeira.
- Comecei a compreender a lógica utilizada no `INNER JOIN`, relacionando `livroFavorito` com `idLivro`.

---

## ⚠️ Dificuldades encontradas

- Confusão inicial ao criar a chave estrangeira na tabela incorreta.
- Alguns erros de nomenclatura de colunas (`idLivro` x `idLivros` e `anoPublicacao`).
- Dificuldade para montar os primeiros `INNER JOIN`, principalmente na identificação das colunas utilizadas no relacionamento.
- Utilização do `LIKE` em uma coluna numérica, reforçando quando seu uso é apropriado.

---

## 🎯 Próximos passos

- Finalizar os exercícios de `INNER JOIN`.
- Praticar consultas envolvendo três tabelas (`Alunos`, `Curso` e `Livros`).
- Continuar fortalecendo o raciocínio sobre relacionamentos entre tabelas antes de avançar para novos conteúdos.

---
💡 **Observação:** Hoje percebi que estou deixando de apenas decorar comandos e começando a entender a lógica por trás dos relacionamentos entre tabelas, 
identificando melhor onde cada informação deve ser armazenada e como elas se conectam.
